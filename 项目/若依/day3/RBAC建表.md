```sql
CREATE TABLE `paismart`.`Untitled`  (
  `id` bigint NOT NULL COMMENT '用户表主键',
  `username` varchar(50) NULL COMMENT '用户名字',
  `password` varchar(255) NOT NULL COMMENT '密码',
  PRIMARY KEY (`id`)
);
```
