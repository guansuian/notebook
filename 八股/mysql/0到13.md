# 什么是 MySQL
MySQL 是开源的关系型数据库，隶属于 Oracle 公司，是国内使用最广泛的数据库之一。我本地安装的是 8.3 版本。

# MySQL 出现性能差的原因有哪些？
- SQL 写法问题：全表扫描、复杂 JOIN/子查询、未命中索引、返回列过多。
- 索引问题：缺失索引、低选择性索引、冗余索引、索引维护成本过高。
- 数据量与表结构：单表过大、冷热数据未分离、表设计不合理。
- 事务与锁：长事务、锁等待、死锁导致吞吐下降。
- 资源与配置：CPU/I/O 瓶颈、内存不足、参数配置不合理。
- 访问模式：热点读写、突发流量、缓存失效导致回源。

## 追问
1. 你提到 SQL 复杂或全表扫描。在 Windows 生产环境里，如何通过工具定位具体慢 SQL？除了 `EXPLAIN`，你会关注哪些指标来判断是 CPU 密集还是 I/O 密集？
2. 高并发更新的表上，B+ 树索引导致严重 Page Split 时，你是否仍会加索引？如何用覆盖索引减少回表，在不增加索引数量的前提下提升 3-5 倍性能？
3. 引入 Redis 后可能出现缓存击穿/雪崩，MySQL 会被打爆。请给出企业级防护方案，并说明在 Spring Boot 中如何实现？
